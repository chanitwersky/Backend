
#לזכור לשנות

# 1. בחירת תמונת הבסיס (במקרה זה Node.js)
FROM node:18

# 2. הגדרת תיקיית העבודה בתוך הקונטיינר
WORKDIR /app

# 3. העתקת קבצי הגדרות הספריות
COPY package*.json ./

# 4. התקנת הספריות הדרושות
RUN npm install

# 5. העתקת שאר קוד המקור של ה-Backend
COPY . .

# 6. חשיפת הפורט עליו רץ השרת (למשל 5000)
EXPOSE 5000

# 7. הפקודה להרצת השרת
CMD ["npm", "start"]